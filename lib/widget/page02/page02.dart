import 'package:flutter/material.dart';

class Page02View extends StatelessWidget {
  const Page02View({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('page02_temp'),
    );
  }
}
