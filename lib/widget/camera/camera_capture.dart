import 'package:flutter/material.dart';

import 'detect_image.dart';

class Camera extends StatelessWidget {
  const Camera({super.key});

  @override
  Widget build(BuildContext context) {
    return const DetectImage();
  }
}
